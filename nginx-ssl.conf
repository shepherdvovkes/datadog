# Default nginx configuration for SSL
# This file will be replaced by ssl-setup.sh with proper SSL configuration

server {
    listen 80;
    server_name _;
    
    # Root directory
    root /usr/share/nginx/html;
    index index.html;
    
    # Basic security headers
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Let's Encrypt challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Language-specific files
    location ~ ^/src/pages/(index-en|index-ja)\.html$ {
        try_files $uri =404;
    }
    
    # Main location block
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Static files caching
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Security.txt
    location /.well-known/security.txt {
        alias /usr/share/nginx/html/security.txt;
    }
    
    # Robots.txt
    location /robots.txt {
        alias /usr/share/nginx/html/robots.txt;
    }
}

# HTTP server for Cloud Run (SSL handled by Cloud Run)
server {
    listen 80;
    server_name _;
    
    # Root directory
    root /usr/share/nginx/html;
    index index.html;
    
    # Basic security headers
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Language-specific files
    location ~ ^/src/pages/(index-en|index-ja)\.html$ {
        try_files $uri =404;
    }
    
    # Main location block
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Static files caching
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Security.txt
    location /.well-known/security.txt {
        alias /usr/share/nginx/html/security.txt;
    }
    
    # Robots.txt
    location /robots.txt {
        alias /usr/share/nginx/html/robots.txt;
    }
}
